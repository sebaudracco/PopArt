package com.example.poparticlestest.main.navigation

import android.app.Activity
import android.content.Intent

class MainNavigation {

    fun navigateToDetail(activity: Activity, idArticle: Int?) {
       /* val intent: Intent = DetailViewViewActivity.getCallingIntent(activity)
        intent.putExtra("Identify", idCharacter)
        activity.startActivity(intent)
        activity.finish()*/
    }
}